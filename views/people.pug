extends ./layout

block content

  ul
    for person in people
      div.row
        li.h4.name #{person.firstName + ' ' + person.lastName}
        a.button(href="/people/" + person.id) Update
        a.button.delete(href="/people/delete/" + person.id) Delete
      
